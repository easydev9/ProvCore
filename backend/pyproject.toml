[project]
name = "provcore"
version = "0.1.0"
description = "ProvCore backend"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
